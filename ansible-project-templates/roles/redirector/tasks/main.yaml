---
- name: Update Cache
  apt:
    update_cache: yes
- name: Install Nginx
  apt:
    name: nginx
    state: latest
#- name: Configure Nginx Redirector
#  script: "{{ role_path }}/files/installer.sh"
